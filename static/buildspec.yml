version: 0.2

phases:
  install:
    commands:
      - echo Installing...
      - pip install flask pytest

  build:
    commands:
      - echo Running tests...
      - pytest

artifacts:
  files:
    - '**/*'